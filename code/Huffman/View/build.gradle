apply from:   rootDir.getAbsolutePath() + '/code/config/scripts/javafx.plugin'

dependencies {
    compile project(':Huffman-ViewModel')
    compile project(':Huffman-Infrastructure')
}

sourceSets {
    main.java.srcDir 'src/main/java'
    test.java.srcDir 'src/test/java'
}
